nse:
  watchlist:
    - TCS
    - INFY
  circular-keywords:
    - "orders"
    - "contracts"
  announcement-keywords:
    - "Bagging/Receiving of orders/contracts"
  ignore-keywords:
    - "Awarding or Bagging of Orders/Contracts-XBRL"

  # poll-interval-ms no longer used — polling is dynamic:
  #   2 min during market hours (IST 09:15–15:30, Mon–Fri)
  #   5 min outside market hours
  ignore-sme: true          # set to false to also receive NSE Emerge (SME) announcements

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN:}
  chat-ids:
    - "5937539323"    # Adil Rizwan Mohammed

# ANNOUNCEMENTS now use a public RSS feed (no proxy needed).
# CIRCULARS use session-based NSE access (no proxy needed).
proxy:
  base-url: ""
  auth-header: ""
  auth-token: ""

analysis:
  openai-api-key: ""

anthropic:
  api-key: ${ANTHROPIC_API_KEY:}   # set this env var on the VM, or paste key directly here

screener:
  username: rizwan.dil6@gmail.com
  password: Adil_12345

screening:
  enabled: true

# ── Daily Market Bulletin ─────────────────────────────────────────────────────
# Sent every day at 7:30 AM IST via Telegram.
bulletin:
  watchlist:
    - CONCOR
    - HATHWAY
    - NHPC
    - JSWINFRA
    - JKIL
    - SUZLON
    - WAAREEENER
    - OLAELEC
    - HINDCOPPER
    - MHRIL
    - RVNL
    - RAILTEL
    - GAIL
    - ADSL
    - ARMAN        # ARE&M — verify NSE symbol if needed
    - NCC
    - STERTOOLS
    - TEXRAIL
    - TITAGARH
    - MANINDS
    - SERVOTECH
    - IRCON
