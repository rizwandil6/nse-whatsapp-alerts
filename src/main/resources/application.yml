nse:
  watchlist:
    - TCS
    - INFY
  circular-keywords:
    - "orders"
    - "contracts"
  announcement-keywords:
    - "Bagging/Receiving of orders/contracts"
  ignore-keywords:
    - "Awarding or Bagging of Orders/Contracts-XBRL"

  poll-interval-ms: 30000   # 30 seconds

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN:}
  # Provide one or more Telegram chat IDs (use @username or numeric ID)
  chat-ids:
    - "5937539323"    # Adil Rizwan Mohammed

# ANNOUNCEMENTS now use a public RSS feed (no proxy needed).
# CIRCULARS still use the JSON API and may need a scraping proxy if NSE blocks your IP.
proxy:
  base-url: ""
  auth-header: ""
  auth-token: ""

analysis:
  openai-api-key: ""

screener:
  username: rizwan.dil6@gmail.com
  password: Adil_12345

screening:
  enabled: true
